# 2026-07-09 工作日志

## PRD 迭代（首页智能体 - 天马智擎）
- 3.2.2 业务流程图改进：专家模式由单箭头「选专家→Plan→Execute→汇总交付物」改为显式 **Plan-and-Execute 循环**——Plan 规划步骤→用户确认（不满意可重规划 S3h→S3g）→Execute 单步循环至全部完成（S3j→S3i）→汇总交付物；同步更新阶段说明表行。文件：`docs/需求文档/20260709-【PRD】AI工作台-首页智能体-天马智擎.md`（V2.1 后续微调）。

## 记忆机制模型讨论与 PRD 修正（3.4.7 / 3.4.8）
- 发现 PRD 原记忆分层有偏差：把 in-session 窗口误称「短期记忆」（项目里短期记忆=LanceDB 向量摘要）、漏写 tier2 对话摘要向量记忆、memoryWindow 错误绑到跨会话。
- 与用户讨论后拍板：保留两层（会话内 / 跨会话），LanceDB 对话摘要向量记忆列为**后续迭代项**（不进 MVP）。
- 改后：3.4.7 会话内记忆（瞬时记忆/Context-Window，双限制：token 硬上限 + memoryWindow 软窗口默认5）；3.4.8 跨会话记忆（持久化，含对话摘要向量记忆[后续迭代]+行为偏好记忆[MVP]）；memoryWindow 仅约束会话内；CoT 仍 3.4.9。同步更新 3.4 引言与附录小结 line 1511。

## 3.2.2 流程图再修正
- 专家模式循环：删掉「用户确认计划?」节点及其「否，调整」回环（用户确认 3.2.2 循环无此步）；现 选专家→Plan→Execute 单步循环→汇总。同步改阶段说明表行。
- 日常办公分支完善：原「检索路由→生成答案」拆为 意图理解→（公开/时效走 web-search 检索 | 企业私有知识走向量数据库 RAG 检索）→融合检索结果+生成答案。表行补充「先意图理解判断信息类型」。
- 留待确认：3.5.5/3.5.8 及术语表 line22 仍写「Plan 供用户确认/可调整后执行」，与 3.2.2 去确认不一致，需用户拍板是否同步去掉。
