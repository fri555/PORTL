# 2026-08-23

## 天马智擎 V1.2 PRD 起草

- 接到任务：基于 11 个 P0 功能点起草 V1.2 PRD，先列大纲落本地 md。
- 参考两份历史 PRD 骨架：
  - 主参考：`20260805-【PRD】AI工作台-日程看板（MVP）-天马智擎项目.md`（章节顺序：版本信息 → 关于本文档(术语/参考/沟通) → 产品概述 → 功能需求(全局规则/流程图/模块清单/功能详情) → 非功能需求 → 迭代计划与 Non-goals → 附录）。
  - 副参考：`mExel2BLV541qeQBipNdbopNWgk9rpMq`（产品文档模版V1.0，AI 专用章节更全）。
- 路径：`docs/需求文档/20260823-【PRD】AI工作台-天马智擎V1.2-天马智擎项目.md`
- 已映射的 11 个 P0 功能点 → 11 个模块（M1–M11），其中：
  - 首页：M1 多模态理解 / M2 文件大小格式 / M3 ReAct 轮次 / M4 历史会话分组 / M5 澄清卡优化 / M6 个人知识库 / M7 基础能力底座 / M11 钉钉 dws 工具
  - 知识中心：M8 下载权限 / M9 知识库细粒度权限 / M10 文件树同步
- 大纲仅列骨架与占位，正文阶段按主参考 PRD 的 EARS 句式、数据实体、规则、接口展开。
- 沟通项待评审：钉钉接口（dws 群选人/生态打通）、AI 工程组（子 agent 契约）、权限平台（继承+黑名单实现）、基础能力底座（五大原子边界）。

## 经验沉淀

- 钉钉 alidocs 链接 WebFetch 拿不到正文（被重定向到登录壳）。正确路径：`dws doc read --node <node_id>` 提取 markdown 字段。
- 命名规范：`YYYYMMDD-【PRD】AI工作台-<模块名>-天马智擎项目.md`，落在 `docs/需求文档/` 下。

## 章节结构调整（依据用户反馈）

- 删除 4.1 全局通用规则（数据保存边界 / 加载空错态 / 并发防重），相关约束沉淀到各功能模块的「业务规则和约束条件」里。
- 在 4.4 功能详情之下新增统一需求模板说明（需求编号、用户角色与用户故事、业务主流程图、分支流程、界面原型、EARS、数据说明、业务规则、接口说明、AI 链路说明），后续每个功能点按此模板展开。
- 补充：每个 4.4.X 现固定 11 个 5 级栏目（1 需求编号 / 2 功能需求描述 / 3 用户角色与用户故事 / 4 业务主流程图 / 5 分支流程(异常) / 6 界面原型 / 7 EARS / 8 数据说明 / 9 业务规则 / 10 接口说明 / 11 AI链路说明可选）；分支流程独立成节，AI 链路说明标可选(N/A)。

## 模板最终定稿（2026-08-23 末轮）
- 按用户要求：删掉 4.4.X.1「需求编号」独立节、删掉 EARS 节；每个 4.4.X 最终固定 **9 个栏目**（功能需求描述 / 用户角色与用户故事 / 业务主流程图及说明 / 分支流程异常 / 界面原型 / 数据说明 / 业务规则 / 接口说明 / AI链路说明可选）。
- 4.3 功能模块清单：仅 5 列（编号/功能名称/功能描述/所属模块/优先级(PN-N)），去掉负责人/状态/排期/所属端。
- 同时补全 1–7 章大框架：术语表(12)、产品目标(11)、竞品对比、非功能(性能8/安全7/兼容4)、迭代计划(11 P0)、Non-goals(5)、附录提示文案占位。

## 套用 dingtalk-prd-from-md 格式（末轮）
- 用户要求按 `dingtalk-prd-from-md` 技能格式调整本地 md。读转换器 `md2jsonml.py` + `jsonml-spec.md` 后确认：封面需用文件头两行纯 `**加粗**`；标题层级整体提一级（`## 一、` → `# 版本信息`），并去掉手写编号让转换器自动编 `1.`/`4.4.1.1`（与 4.4.X 模块/9 子节层级天然对齐）；TOC/灰表头/12pt 自动生成。
- 已整体重写本地 md 为转换友好结构，并本地跑 `md2jsonml.py` 验证：184 blocks、cover 两行正确、TOC 7 节点、编号 版本信息(h1)/多模态理解(h3→4.4.1)/功能需求描述(h4→4.4.1.1)、10 张表。验证通过，临时 jsonml 已删。
- 注意：推送钉钉为真实账号写操作，按用户规矩需先确认关键参数再执行（`dws doc update --node <id> --mode overwrite`）。

## 对照 20eM… 参考 PRD 收尾格式（本轮）
- 用户要求按 20eMKjyp81RPqEQBfeLZRlEqWxAZB1Gv 这个 PRD 的格式再调本地 md。该参考结构：沟通要求表、用户故事地图表、竞品留空、功能详情标题用 `M2-日程看板` 形式、界面原型等用固定列宽表。
- 本轮改动：① M10/M11 对调（M10=钉钉dws/P0-10，M11=文件树同步/P0-11）；② 功能详情标题去掉 (M1 / P0-1 / 首页) 后缀，改为 `### M1-多模态理解`；③ 沟通要求→表格；④ 用户故事地图→表格；⑤ 产品目标→简要编号清单（去掉 G 表）；⑥ 竞品调研留空；⑦ 在 4.4 模板区为 界面原型/数据说明/业务规则/接口说明/AI链路说明 各给出示例表结构。
- 本地跑 `md2jsonml.py` 复验：183 blocks、TOC 7 节点、h3 中 M10-钉钉 dws 工具 / M11-文件树同步 正确、11 张表。验证通过，临时 jsonml 已删。
- 下一步：用户评审后挑模块展开 9 栏目正文；定稿后再推钉钉（需先确认 node id / 新建还是覆盖）。

## 推送新建钉钉文档 + 更新技能（本轮，10:54）
- 用户确认「更新技能 然后新建一个钉钉文档」，明确授权新建（非覆盖）。
- 本地 md 转 jsonml：`python md2jsonml.py ... -t "V1.2 产品需求说明书（PRD）"` → 183 blocks。
- 新建钉钉文档：`dws doc create --name "V1.2 产品需求说明书（PRD）- 天马智擎" --content-file _v12.jsonml --content-format jsonml -y` → 成功，nodeId `gvNG4YZ7Jne0ZKkAcAa00ajXV2LD0oRE`，URL https://alidocs.dingtalk.com/i/nodes/gvNG4YZ7Jne0ZKkAcAa00ajXV2LD0oRE 。create 返回的 warnings（rPr/numPr/hRule/pageBreakBefore）均非致命，样式保留。
- 读回验证：`dws doc read --node ...` 返回 10526 字符，含 V1.2/多模态理解/功能模块清单/用户故事地图/沟通要求/竞品调研 全部章节，确认落地成功。临时 jsonml 已删。
- 更新技能 `dingtalk-prd-from-md`：在 SKILL.md 新增「Local Markdown authoring conventions (V1.2 practice)」节，固化封面两行加粗 / 标题提一级去手写编号 / 功能详情标题 `M{n}-名` / 模块清单5列 / 沟通要求·用户故事地图用表 / 产品目标精简 / 竞品留空 / 每功能9栏目 / M与P0编号对齐 等可复用规范。注：SkillManage 工具调不起，直接用 Edit 改本地 SKILL.md（agent_created:true，允许）。

## 全量填橙 + 覆盖推送 + 收尾核验（末轮）
- 用户要求：界面原型/数据说明/业务规则和约束条件/接口说明/AI链路说明 每个功能点都给示例表、标橙色；并修正首页（目录前）封面格式，参考 MNDoBb60VLrZLmzBSmvN7QEL8lemrZQ3。
- 修封面：由 `**天马智擎 AI 工作台**`/`**V1.2 产品需求说明书（PRD）**` 改为 `***天马智擎项目***`/`**V1.2 产品需求说明书（PRD）**`（参考文档首行用 `***` 三花，26pt 斜体封面标题）。
- 11 个模块（M1–M11）9 栏目全部展开，示例表前用 `<!-- 示例 -->` 标记（转换器据此把表头 fill 设为 `#FB8C00` 橙 + 白字；标记自复位，后续表回灰色 `#C0C0C0`）。AI链路说明对 M4/M5/M8/M9/M10/M11 标 N/A（无表）。
- 改转换器 `md2jsonml.py` 支持橙色示例表：新增 `pending_example` 标志 + `cell_p(items, header, orange)` + `make_table(rows, orange)`；`<!-- 示例 -->`/`<!-- orange -->`/`<!-- 示例表 -->` 命中即置橙并复位。这是核心技能升级，可复用。
- 覆盖推送：`dws doc update --node gvNG4YZ7Jne0ZKkAcAa00ajXV2LD0oRE --content-file _v12b.jsonml --content-format jsonml --mode overwrite --yes` → `success: True`。
- 收尾核验：源 md 20112字/49橙色标记/11模块；转换器重跑得 186 橙 cells（49表）+39 灰 cells（11表）；钉钉读回 11 模块+界面原型+业务规则均落地。临时 `_v12b.jsonml` 已清理。最终文档 https://alidocs.dingtalk.com/i/nodes/gvNG4YZ7Jne0ZKkAcAa00ajXV2LD0oRE 已就绪待评审。

## 三处优化（本轮，11:15 后）
用户反馈：① 橙色应设在示例表「所有内容文本」而非仅表头填充；② 删掉「功能详情」后紧跟的注释块；③ 「版本信息」放目录前、不设多级编号。
- 转换器 `md2jsonml.py` 调整：
  - `cell_p`：orange 时把 rpr color 设为 `#FB8C00` 应用到**所有单元格**（表头+数据），去掉原先「orange 仅表头白字」逻辑。
  - `make_table`：orange 表头**不再填充**（去掉 `#FB8C00` fill），仅 schema 表保留灰色 `#C0C0C0` 表头填充 → 示例表=纯橙字无底色，schema 表=灰底黑字，视觉区分清晰。
  - 新增 `<!-- preface -->` 前言机制：`heading_block` 增加 `numbered` 参数（False 时不加 numPr/list，不编号）；`convert` 中 `<!-- preface -->` 后到下一个一级标题之间的区块渲染为不编号前言，且**不进 TOC、置于 TOC 之前**。参考文档 MNDoBb… 的「版本信息」即此类（封面后、目录前、纯标题不编号）。
- 源 md：在 `# 版本信息` 前插入 `<!-- preface -->`；用脚本删除 `## 功能详情` 与 `### M1` 之间的整段 blockquote 注释（模板说明+示例表结构）。
- 核验：重跑得 leaf 颜色 `#FB8C00`×778（示例文本）、`#C0C0C0` fill×39（schema 表头）、无橙填充；文档顺序=封面→版本信息(h1,无numPr)→版本变更记录表(h2)→TOC(从「1. 关于本文档」起,不含版本信息)→正文；回读确认版本信息位置(47)<关于本文档(397)、功能详情→M1 无残留注释、示例表动作列存在。
- 覆盖推送：`dws doc update --node gvNG4YZ7Jne0ZKkAcAa00ajXV2LD0oRE --mode overwrite` → success:true。临时 `_v12c.jsonml` 已清理。

## 13:17 用户指定 PRD 模板 + 冻结 V1.2 实稿

- 用户新建专用 PRD 模板供参考：`R1zknDm0WR3Q7YRDSzOP055YVBQEx5rG`（https://alidocs.dingtalk.com/i/nodes/R1zknDm0WR3Q7YRDSzOP055YVBQEx5rG）。已读回吸收完整骨架（封面 `***产品需求说明书***`/`**天马智擎项目**`；版本信息为目录前普通标题；含术语表/参考文档/沟通要求/产品概述/产品目标/用户角色/竞品/功能需求[流程图:技术架构图+业务流程图,功能模块清单,M1–M11 各 9 个 `####` 子节]/非功能需求/迭代计划与Non-goals/附录）。
- 指令①：以该模板为今后所有 PRD 基准格式 → 更新技能 SKILL.md「Canonical baseline」指向模板节点 + 完整大纲；MEMORY.md 加 ⭐ 基准格式锁定。
- 指令②：刚建的 V1.2 实稿 `gvNG4YZ7Jne0ZKkAcAa00ajXV2LD0oRE` **冻结，不要乱动**（用户直接编辑）→ MEMORY.md 加 🚫 冻结规则：严禁再对该节点 `dws doc update`，除非用户明确说"推/覆盖/同步"。
- 注意：模板封面/版本信息写法与我之前的 md2jsonml 约定略有差异（模板封面=产品需求说明书/天马智擎项目；版本信息普通标题非 `#`），以模板节点为权威基准。
