# 2026-07-10 工作日志

## 企业级 Agent 平台额度管控 / 内部金币体系调研
- 任务：深度调研 AI Agent 企业级 token 额度管理，结合自研 Agent 平台设计金币(虚拟积分)挂钩体系。计划底座：DeepSeek V4 + 阿里百炼。
- 产出：`docs/agent平台额度管控与内部金币体系调研报告.md`（已用 2026-07 真实数据校验并修正草案）。
- 关键发现/约束：
  1. 🔴 阿里百炼 Token Plan 团队版 ToS 禁止「后端自动化/应用后端」调用，仅限交互式工具。自研后端多租户平台必须走百炼按量付费/资源包/节省计划，不能用 Token Plan 坐席。
  2. 🔴 DeepSeek V4 正式版(7月中旬)引入峰谷定价：高峰 9:00-12:00、14:00-18:00 价格 ×2（Pro 平时输入3/输出6 → 高峰6/12 元每百万token）。金币锚定需按高峰价或预留≥30%缓冲。
  3. DeepSeek V4 Pro 当前永久 2.5 折（官方价 1/4），草案成本估算偏高 3-7 倍，已用真实单价重算。
  4. 行业共识：统一 Credits 抽象层(0.01-0.05元/积分) + 三级配额(企业→部门→个人) + 共享池+个人独占配额 + 超额三模式(扣款/拦截/重置) + 令牌桶多时间窗+成本熔断工程层。
- 建议金币方案：10金币=1元(高面额直观)、每日赠50金币/人(≈¥150/人/月)、单任务硬上限500金币、扣减按实际token×实时单价×难度乘数(非固定系数)。
- **用户澄清(第二轮)**：①金币是既有内部体系(可换奖品)、**10:1比例固定不可动**，不从比例优化，从别处优化；②消耗金币**四舍五入取整**(保留1位小数、单次最低0.1)，不追小数精度；③抵扣优先级=每日赠送额度(优先)→[后续]金币兑换token，**MVP不做金币兑换**；④**MVP限额只到用户个人层级**(每日+可选月度)，企业/部门/项目分层与共享池为后续阶段；⑤报告所有具体信息须附**可验证引用来源**(已加内联脚注+文末来源表，外部链接+内部/草案标注)。
- MEMORY.md 项目长期记忆已同步本轮约束。

## frontend-mvp 专家模式卡片对齐（3×3 同尺寸 + 分页）
- 根因复盘：用户反复反馈"看不到变化/卡片大小没变"。真相是 **源码被回退成旧版**（ExpertPicker 仍是响应式 2/3/4 列、案例纵向整行列表、无分页），且**有僵尸 vite preview 进程在 4800/5000/6060 上自动重启吐旧构建**，导致预览永远显示旧画面。
- 修复：`ExpertPicker.vue` 重写为专家卡片与案例卡片**完全相同瓦片**（`grid-cols-3` 固定 3 列、`min-h-[220px]` 等高、同圆角同内边距）；两个网格均 `PAGE_SIZE=9` 分页（上一页/页码/下一页）；专家网格加名称搜索。`experts.ts` 扩为 **10 专家 × 各 10 条案例**（E10 老纠=质量合规专家，按 PRD 6.1），使专家与案例网格都出现第 2 页演示分页。
- ⚠️ **预览铁律（务必遵守）**：改 frontend-mvp 后，先 `for p in 4321 4800 5000 6060 5173; do kill -9 $(lsof -ti:$p) ; done` 杀光所有旧预览/dev 进程，再用 `npx vite build --base=/` + `npx vite preview --port <全新端口> --host 127.0.0.1` 起**唯一**新服务。vite base 默认 `/PORTL/`（旧端口都带此前缀，易混淆），构建用 `--base=/` 最干净。绝不要依赖 dev HMR（预览面板不刷新）。旧端口 kill 后可能短暂 403/又 200（僵尸重启），需反复确认到 000 才算干净。
- 当前唯一有效预览：`http://127.0.0.1:7070/`（根路径）；专家模式在 `/workspace/chat` → 模式切换选「专家模式」。

## 修正：专家/案例卡片改动目标应为「首页」而非 workspace 专家选择页
- 用户明确指出：之前一直在改的 `ExpertPicker.vue`（位于 WorkspaceChatView / `/workspace/chat`）是"新建的页面"，他要改的是**首页 `HomeView.vue`** 的专家模式卡片。
- 已 `git checkout` 回退错改的 `src/components/workspace/ExpertPicker.vue` 与 `src/mock/experts.ts`（恢复到基线，保证 workspace 仍可构建），把改动聚焦到首页。
- 首页改动（`src/views/HomeView.vue`）：
  - `expertAgents` 由原型里的 组货/分析/营销/设计/运营 +「敬请期待」占位，替换为 PRD 的 **10 位电商专家**（选品王/带货一姐/飞毛腿/点子王/解忧姐/大表姐/大客户一哥/操盘手/老王/老纠），每位带 10 条 prompt 案例（共 100 条 mock）。
  - 专家卡片与案例卡片**统一为同一尺寸瓦片**（`min-h-[172px]`、`grid-cols-3`、同圆角同内边距）。
  - 专家网格与案例网格均 `PAGE_SIZE=9` 3×3 分页（专家 10→2 页；案例 10→2 页），含上一页/页码/下一页；`watch(selectedExpert/runMode)` 自动重置页码；选中专家后「返回专家列表」回到专家网格。
  - 仅在 `runMode==='task'`（专家模式）下展示；日常办公模式案例卡片保持原样。
- 验证：`npm run build` 通过（vue-tsc 无类型错误）；`npx vite preview --port 7070 --host` 启动，访问 `http://127.0.0.1:7070/PORTL/`（vite base 默认 `/PORTL/`），登录用 Demo「模拟扫码登录」，首页切「专家模式」查看。

## 修正：首页卡片尺寸再缩小 + 统一，日常办公模式追加分页
- 用户二次反馈：专家卡片与案例卡片「大小还是不一样，太大了」；并要求**日常办公模式**也增加 mock 案例卡片并展示翻页效果。
- 首页 `HomeView.vue` 再改：
  - 专家卡片与专家模式下的案例卡片**统一缩小到完全相同尺寸**：`min-h-[150px]`、`p-4`、`rounded-2xl`、同 `.border-zinc-200`，专家 emoji 由 h-16 缩到 h-12，案例 desc 由 line-clamp-4 收到 line-clamp-3，两者 box 完全一致。
  - **日常办公（quick）模式**：原 `visibleCases` 只有 ~3 条且走已废弃的 `activeCaseCategory` 分类（无可见 tab），无分页。新增 `dailyOfficeCases`（13 条 mock），`pagedDailyCases` + `dailyPage` + `dailyTotalPages`（PAGE_SIZE=9 → 2 页），用 `v-else`（quick 分支）渲染 3×3 网格 + 上一页/页码/下一页；`watch(runMode)` 里重置 `dailyPage`。日常办公卡片保持紧凑横向（icon+文字，`min-h-[96px]`），与专家模式卡片形成模式区分。
  - 定时任务分支（schedule）本就由外层 `v-if="runMode!=='schedule'"` 排除，单独 `schedule-panel` 渲染，故移除我误加的 `v-else-if="runMode==='schedule'"`（TS 报不可达类型错误）后构建通过。
- 验证：`npm run build` 通过；预览 `http://127.0.0.1:7070/PORTL/`，首页切「日常办公」可见 13 条案例 3×3 分页（第2页4条），切「专家模式」专家/案例卡片均为 150px 同尺寸分页。

## 第三轮修正：卡片尺寸对齐日常办公卡 + 案例恢复气泡样式
- 用户反馈：①日常办公案例卡片「刚刚好」（1920×1080 下 3×3 不滚动），但专家模式专家卡片还比它大 → 真相是日常办公卡是 `min-h-[96px]` 横向紧凑样式，专家卡之前是 `min-h-[150px]` 竖向大卡，所以不一致。②专家卡片点击后的案例被我改成了瓦片，用户要**保持原气泡样式**。
- 修正：
  - 专家模式专家卡片改为与日常办公卡**完全一致尺寸**：`min-h-[96px]` 横向（`flex items-center p-4` + 左侧 `h-10 w-10` 渐变 emoji 圆角盒 + 右侧 标题/角色/描述），容器同为 `max-w-[920px] grid-cols-3 gap-3`，保留 3×3 分页（10 专家→2 页）。
  - 专家卡片点击后的案例区**恢复气泡样式**：`flex flex-wrap justify-center` + `rounded-xl bg-zinc-100` 不定宽气泡（按 `index%5` 给 5 种宽度），显示 `item.bubble ?? item.title`，无分页（与原一致）。保留「返回专家列表」和顶部专家信息卡。
  - 日常办公案例卡片不动（即尺寸基准）。
- `npm run build` 通过；预览重启 `http://127.0.0.1:7070/PORTL/`。已将「首页卡片尺寸真理（日常办公卡=min-h-[96px] 横向）」与「两段式 UI 约定（专家网格→气泡案例）」写入 MEMORY.md。
