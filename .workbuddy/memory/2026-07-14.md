# 2026-07-14 工作日志

## 钉钉表格·用户故事地图-按角色 v4 重做（横向泳道）
- 用户强烈不满 v3（扁平7列但泳道竖向串列），要求改为：**4 大环节(发现/持续/沉淀/反馈)作为横向泳道**，每泳道内列=分组|用户故事场景|功能点|子功能|功能描述，**角色只在最前面 A 列（合并一次），后续列不重复**。
- 最终落地（文档② nodeId=7NkDwLng8ZMxdgP2TN0Q61L5JKMEvZBY, sheetId=st-c228fdf9-714705）：6 列 A-F，11 角色区，每角色 4 条横向泳道整块着色，共 285 行。标题金、角色列蓝、泳道黄/绿/紫/灰。
- 关键技术决策：
  - 数据分 3 块 d1(A1:F95)/d2(A96:F190)/d3(A191:F285) 用 update_range 写值（d3 本次补齐）。
  - **弃用"手写几百行 backgroundColors 二维数组"**（易漏行报错 "data has 279 but range has 283"）；改用**调色板(Z列 1×5 色块) + fill_range 平铺**：fill 会 tiling，1×5→88×5、1×1→283×1 均成功。44 条泳道 + 角色列各一次 fill，tiny 调用。
  - 大 JSON 防 Read 截断：把数组写成**每行一个子数组**（per-row），单文件 <2000 行即可完整读出，再原样作为工具参数发出。
  - 合并 56 个（标题 A1:F1、44 泳道头 B:F、11 角色列 A）走 4 批 batch_update(≤18/批)。
- 校验：回读 A1:F6 与 A190:F197，确认角色列不重复、泳道头合并、故事行结构正确。
- 临时脚本/文件在 /tmp/usm_h/（gen_ops2.py、bands_fill.txt、d3p.json 等）。
