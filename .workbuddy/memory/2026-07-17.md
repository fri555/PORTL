# 2026-07-17

## 用户故事地图 sheet 改造（角色分组横列表）
- 用户要求：把 `用户故事地图` sheet 前三行表头保持不变（角色 + 1/2/3/4 + 发现/持续/沉淀/反馈 + 分组×4），**增加角色分组**——即数据按角色分组、角色列填上。
- 目标 sheet：钉钉 文档② `7NkDwLng8ZMxdgP2TN0Q61L5JKMEvZBY` / `用户故事地图`(st-672db32a-82278)。原 `用户故事地图-按角色`(st-c228fdf9-714705) 已不在 doc 中。
- 做法：用 `用户故事地图-按角色-v3.csv`(239条, 角色|泳道|分组|场景|功能点|子功能|描述) 生成 21 列横表（角色A + 4×5环节列），245 数据行按 11 角色分组（普通用户189/系统管理员31/商品运营17/HR1/财务1/余6岗占位）。
- 数据+角色合并由子 agent 写入成功；配色+表头加粗+重合并由子 agent 二次执行成功。
- **关键坑**：`fill_range` 对合并单元格极度敏感，unmerge 整片后仍报 mergecells。最终改用 `update_range` backgroundColors 二维数组（对合并安全）逐块上色，绕开 fill_range。已更新 MEMORY.md 钉钉坑位。
- 我的 Bash 本会话持续报参数序列化错误（所有命令被拒），改用子 agent（独立上下文）跑 Python 生成脚本 + 写钉钉，自己用 DeferExecuteTool 校验/补色。

## 并行布局改造（修复「空行过多」）
- 用户反馈：21 列横表虽按角色分组，但仍是**纵向堆叠**（每角色每个环节的故事各占一整行，仅该环节列有值、其余 15 列空白）→ 视觉满屏空行。
- 改为**并行布局**：每个角色按 max(各环节故事数) 行，发现/持续/沉淀/反馈 四环节横向并排同一行。普通用户 189→87 行，整体 245→142 行（省 103 行）。
- 脚本 `/tmp/usm_h/gen_parallel.py` 读 v3.csv 生成 par_data_1/2.json + par_bg_1/2.json + par_role_merges.txt（多行 JSON 便于 agent 完整读取）。
- 子 agent 清空 A4:U500 → 写并行数据+背景色 → 合并角色列(A4:A90/A91:A121/A122:A137/A138~A145) → 全量 diff 校验(142×21 与源 0 差异，修正 H139 漏字「对」)。
- 本次新会话 Bash 已恢复，可直接跑 python；写入钉钉仍派子 agent（避免手动塞大数组进工具参数）。
