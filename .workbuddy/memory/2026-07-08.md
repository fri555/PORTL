# 2026-07-08 工作日志

## 天马智擎 frontend-mvp 前端 UI 重写（参照31张设计图）

### 任务概述
参考 31 张「天马智擎」设计图，对 `frontend-mvp` 聊天页做全面 UI 重写：黑白灰浅色设计系统、独立侧边栏、欢迎页快捷卡片、重写输入框（附件横滑/字数上限/粘贴转txt/定时任务面板/专家卡片网格/提示词带入）、双Tab上传弹窗（本地文件/知识中心）、搜索弹窗（左分组结果/右对话预览/空状态）、AppHeader加金币(776)+tooltip、移动端抽屉式侧边栏。

### 关键架构决策
- `useAIChat` 已为单例（`_instance`），子组件通过 `useChatController` 工厂函数 + `provide/inject` 共享状态
- 新建控制器 `src/composables/useWorkspaceChat.ts`，集中 UI 状态与方法；`WorkspaceChatView.vue` 从 1200 行巨石组件瘦身为组装层
- 新增组件：`WorkspaceSidebar` / `ChatMessageList` / `ChatInputBox` / `AttachmentUploadModal` / `ExpertPicker` / `SearchDialog`
- 用户消息气泡改为白底灰边框（非原蓝色）；主操作按钮用 `bg-zinc-900`

### 验证结果
- `npm run typecheck` 0 错误
- `npm run build` 成功（2521 模块，WorkspaceChatView 86KB chunk）
- 修复的关键错误：`sendMessage` 参数顺序（content 在前）、`moreMenuItems` 重复声明、`statusBadge` 空值、函数 ref 类型

### ⚠️ 配色最终定调（用户二次确认）
- 用户给的详细 UI 规格书里写「主色 品牌绿 #10B981」，但**用户明确撤回绿色，定调「黑白为主、黑色主按钮」**（与最初计划一致）
- 已在 `ChatInputBox.vue` 把 4 处 `emerald-500` 品牌用法全部回退为 `zinc-900`（模式激活按钮/联网激活/专家tag/输入框聚焦边框）
- **文件类型小图标色保留语义色**（PDF红/DOC蓝/TXT黄/MD青/xlsx青）——属辅助识别色，非品牌绿，用户未反对
- 教训：规格书/文档里的配色建议不等于用户真实意图，涉及主色变更务必二次确认；本项目天马智擎前端主色 = 黑白灰

## 专家模式专项重构（专项设计文档 V1.0）
- 问题：专家卡片网格原本只是输入框下方的小面板，未按规格书「渲染到主内容区取代欢迎页快捷卡片」
- 修复：
  - `ChatMessageList.vue` 专家模式欢迎区改为：未选→引导文案+`ExpertPicker`网格；已选→「XX在线」提示区+推荐提问（从 `expert.prompts` 读取，点击填充输入框并聚焦）
  - `ChatInputBox.vue` 移除下方 ExpertPicker/推荐提问面板；专家模式未选→禁用发送+「请先选择一位专家」提示；已选→持久「在线」横幅(✕取消)；placeholder 改「向 XX 提问...」
  - `ExpertPicker.vue` 卡片结构对齐规格书：标题16px加粗+角色副标题+1行描述(line-clamp-1)，hover方向性展开显示完整描述，选中2px黑边+浅

### 按规格书补齐剩余模块（黑白主调）
用户确认「不要绿色、黑白为主」后，按 9 模块规格书补齐剩余项（保持 zinc 灰阶 + 黑色主按钮）：
1. **侧边栏状态色点**：`statusBadge` 运行中=蓝点(`bg-blue-500 animate-pulse`)+「运行中」蓝字，完成=灰点(`bg-zinc-300`)+「完成」灰字；对话项 `min-h-[52px]`
2. **欢迎页快捷卡片**：从 3 张纵列扩为 6 张响应式网格（sm:2 / lg:3 列），点击改为「填充输入框+聚焦」(`fillAndFocus`)，不再自动发送
3. **上传弹窗**：本地文件加格式校验(ALLOWED: pdf/doc/docx/txt/md/xlsx/xls/csv) + 100MB 大小校验，非法文件红框+原因、确定按钮禁用；拖拽区 `dragActive` 高亮(`border-zinc-900 bg-zinc-50`)；知识中心 Tab 改为「左侧目录树(个人空间/共享空间+子文件夹) + 面包屑(`kbBreadcrumb`) + 右侧文件网格(`kbPageFiles`) + 分页(共N项/第X页, `changeKbPage`) + 无结果空状态」
4. **专家网格**：`ExpertPicker` 改为 `lg:grid-cols-4`，卡片 `h-[140px]`，hover 方向性展开面板（`hoverAlign(idx%4)`：第1/2列左对齐右扩、第3列居中、第4列右对齐左扩，防溢出），选中边框 `border-zinc-900`
5. **控制器补字段**：暴露 `kbFilteredAll`、`kbSelectedCount`（=localFiles.length），供弹窗分页/计数使用
- 验证：`npm run typecheck` 0 错误；dev server 5177 正常
- 未做（待用户确认是否需要）：规格书模块6.3「定时任务左右分栏管理视图」(任务列表+右侧Cron配置)——当前仅实现输入框内联 frequency/date/time 面板，与设计图一致；完整任务管理视图工作量较大，未自动扩展

### 注意
- 文件上传/知识中心为前端 mock，未接真实 API
- 设计图均为桌面端(~1440px)，移动端为功能可用版（抽屉侧边栏），非像素级还原

### 三模式输入框统一（二次优化）
- 问题：三个模式主界面工具栏不一致——多了一个写死的「日程办公」独立按钮（仅定时任务模式出现），模式按钮顺序错乱（被挤到联网后），placeholder 未区分三种模式，专家模式选中后缺「推荐提问」标签行
- 修复：`ChatInputBox.vue` 重写——三模式共用同一工具栏骨架（`+` | 模式切换按钮 ▾ | 联网 | 字数 | 发送）；模式按钮文案/图标随当前模式变化（日常办公⚡/专家模式👤/定时任务⏰），非默认模式时按钮变 `bg-zinc-900` 高亮；下拉菜单带描述+当前态勾选；placeholder 三模式各异；专家模式选中后展示「推荐提问」可点击标签（带入输入框）；定时任务面板频率/日期/时间内联
- 验证：`npm run typecheck` 0 错误；dev server 5177 正常

### 搜索弹窗统一（用户纠正：搜索界面是弹窗，非输入框）
- 误判：用户说「三个模式主界面的搜索框还不一致」时，我先改了底部 `ChatInputBox`（错的）；用户纠正「搜索界面是一个弹窗的」
- 正解：搜索 UI 是「搜索或更多」弹窗（`SearchDialog.vue`），需三模式一致
- 修复：
  1. 侧边栏搜索图标原本打开内联筛选框（`searchOpen` 内联 input），改为调用 `chat.openSearchDialog()`，与顶栏搜索统一打开同一个弹窗
  2. `SearchDialog` 补成设计图「搜索或更多」：placeholder 改「搜索或更多」；**空搜索时右侧显示「更多」能力网格**（新对话/切专家模式/切定时任务/切日常办公/知识库/MCP），**输入关键词后右侧显示对话内容预览**（左栏按今天/昨天/更早分组结果）
- 验证：`npm run typecheck` 0 错误

## AI工作门户首页智能体 PRD撰写（MVP）

### 任务概述
为集团内部「AI工作门户」撰写首页智能体MVP版本PRD，通过产品战略团队5名成员协作完成。

### 关键设计决策（已与产品负责人确认）
- 三模式Tab手动切换：日常办公/专家模式/定时任务，不做自动意图识别
- 首页即工作台：不单独做全屏对话页面
- 专家模式MVP：搭框架+2个通用专家（写作助手、会议纪要整理）
- 定时任务MVP：3个模板（日报/周报/巡检），钉钉为唯一推送通道
- PRD范围：首页整体布局 + 智能体三种模式完整交互逻辑

### 交付物（7份文档）
位于 `deliverables/product-strategy/`：
1. `prd-portal-agent-2026-07-08.md` — 完整PRD（1379行，EARS需求+Mermaid流程图+界面原型表）
2. `user-research-portal-agent-2026-07-08.md` — 用户研究综合报告
3. `competitive-analysis-portal-agent-2026-07-08.md` — 竞品调研报告（6个竞品）
4. `metrics-framework-portal-agent-2026-07-08.md` — 产品指标框架（25项指标）
5. `priority-timeline-assessment-2026-07-08.md` — 优先级与时间线评估（析客产出，10周/6模块）
6. `roadmap-portal-agent-2026-07-08.md` — 路线图评估报告（路径产出，RICE评分+12周三里程碑+风险矩阵）
7. `prd-portal-agent-mvp-2026-07-08.md` — 最终交付报告（主理人汇总）

### 技术要点
- PRD格式参考已有知识中心PRD（`/Users/richelleshi/Desktop/20260707-【PRD】AI工作台-知识中心（MVP）-天马智擎项目.md`）
- 用户故事地图来源：`/Users/richelleshi/Desktop/AI工作门户-用户故事地图.xlsx`
- 北极星指标：WECT（周有效任务完成数）
- 开发周期：10周，关键路径 M0→M1→M3→M5→M6
- 核心风险：钉钉API对接、大模型回答质量调优

### 团队协作
- 团队名：product-portal-agent
- 成员：user-researcher, competitive-analyst, data-analyst, requirement-analyst, roadmap-planner
- 工作流：研究并行（瑞思+竞析+数析）→ PRD撰写（析客）→ 优先级评估（析客）→ 汇总整合（主理人）

## PRD 重写（参照 frontend 原型 + 6 项修改）

用户要求：核心文件就是 PRD，别搞跨文档引用，参照 `frontend/` 原型重写，按钉钉文档格式。

### 6 项修改点（已全部落实）
1. 用户角色仅普通员工（移除系统管理员/开发者；原型"创建智能体"向导 `isDeveloper` → 不在 MVP）
2. 自动化任务不以日报/周报举例（改用巡检/监测/采集/汇总）
3. 反馈入口（点赞/点踩）不在 MVP
4. 竞品调研排除 Notion AI / Copilot（仅钉钉/飞书/Coze/Kimi/通义）
5. 历史会话侧边栏在左侧（原型已 248px 左栏）
6. 即便全 P0 也要分优先级顺序（P0-1~P0-8 交付序列）

### 与原型对齐的关键决策
- 配色：原型阿里橙 #FF6A00 → 用户拍板**黑白配色**（墨黑 #1A1A1A 主色），参考 PRD 的品牌绿 #10B981 也弃用
- 模式：原型 runMode（快速/智能/任务）+ 左栏面板 → 用户拍板**顶部三模式 Tab（日常办公/专家模式/定时任务）**
- 三栏布局沿用原型：左会话列表 248px / 中对话 / 右引用产物 280px（均可折叠）
- 快捷指令：生成钉钉待办 / 生成需求草稿 / 封装成技能
- 空态文案："输入你的需求，我会帮你完成方案"
- 专家 MVP 内置 2 通用专家：写作助手 + 会议纪要整理
- 定时任务模板 3 个（非日报周报）：竞品动态监测/系统数据巡检/周期资讯采集汇总

### 交付
- 重写 `prd-portal-agent-2026-07-08.md`（钉钉格式：产品概述→全局规范→框架布局→功能模块→边缘场景→数据结构→NFR→埋点→优先级+Non-goals→附录）
- 用户明确：写好 PRD 比其他事重要，未再 spawn 团队，直接由主理人产出

## PRD 再次打回重写（知识中心大纲，终版）

用户指出钉钉格式版"大纲结构不得改变"，要求严格按知识中心PRD大纲重写。已按 `20260707-【PRD】AI工作台-知识中心（MVP）` 大纲重建。

### 大纲结构（与知识中心对齐，零偏离）
版本信息表 → 关于本文档(术语/参考文档/沟通要求) → 产品概述(产品概述/产品目标/用户角色/竞品调研) → 功能需求(全局要求/流程图/功能模块总表/功能详情) → 非功能需求(性能/安全/兼容) → 迭代计划(MVP优先级表/后续迭代) → 附录(成功指标/埋点) → Non-goals

### 关键落点（对照用户6项+2项补充）
- 删设计规范/Design Tokens（零残留）
- 右侧栏 MVP 仅文件预览（默认48px收起，点文件卡片展开280px，对齐豆包；无引用/产物面板）
- 左栏历史会话列表(248px/折叠48px)
- 三模式手动Tab(日常办公/专家模式/定时任务)
- 仅普通员工（管理员/开发者向导全排除）
- 自动化模板=竞品动态监测/系统数据巡检/周期资讯采集汇总（正文无日报/周报，仅Non-goals与"非日报/周报"声明出现）
- 竞品剔除 Notion AI/Copilot（仅钉钉/飞书/Coze/Kimi/通义）
- 全P0仍给 P0-1~P0-8 交付序列

### 验证（本次会话复核）
- 文件 1026 行、19 个 mermaid 图、Non-goals 在 line 1015
- 6 项修改点 grep 全通过；设计规范/Notion/Copilot 仅以"已剔除"声明出现
- 路径：`deliverables/product-strategy/prd-portal-agent-2026-07-08.md`

## PRD 系统指令强制修订（19:19，覆盖上一版）

用户以"系统指令"形式下达最终唯一依据，强制全量修订：
- 参考文档/沟通要求 留空（验证 line 24/26 仅标题无内容）
- 结构扁平化：全文仅一级标题（28 个 #），0 个 ##/###；注意"扁平化"一词本身属禁用语已移除
- UI 设计隔离：零色彩值/色系/视觉风格词（墨黑/主色/毛玻璃等全清除，grep=0）
- 功能移除：彻底删除定时任务/定时提醒全部模块（仅版本说明与 Non-goals 以"移除"措辞出现）
- 默认态：仅搜索框，左/右栏均默认完全收起（推翻旧"左栏展开"逻辑）
- 删除"建议更大屏幕"类措辞（grep=0）
- 专家交互：Hover=文字介绍/履历摘要；Click=完整 Prompt 案例库（严格区分）
- 新增章节：#22 思维链(CoT)设计、#23 记忆系统(Memory)设计（会话内滑动窗口5轮 / 跨会话保留历史搜索词+专家偏好）
- EARS 强制：17 个功能描述段均含 普遍型/状态驱动型/事件驱动型/不良行为型（各 17 次，平衡）
- 粒度：每个模块均含 正常操作流 + 异常回退流 + 异常处理（覆盖断网重试/空态/骨架屏/非法字符/并发冲突）
- 验证：815 行；多级标题 0；视觉风格词 0；定时任务仅 2 处"移除"措辞
- 用户明确要求忽略此前所有口头描述，以本指令为唯一依据，故未走团队编排、主理人直接产出

## Dev server 排查：用户「看不到变化」真相（18:30）
- 现象：用户反馈 frontend-mvp 改完后 UI「和之前一样、没变化」
- 排查：typecheck 0 错误；磁盘代码确认已更新（ChatMessageList 含未选专家网格、ChatInputBox 主按钮已回退黑白、emerald 仅剩文件类型语义色）；旧 dev server 在 :5177 仍返回 200
- 根因：:5177 端口叠了 2 个进程（PID 16182/27028），HMR 早已失联，用户标签是浏览器缓存的旧页
- 解决：沙箱拦截了 `lsof/kill` 端口释放，故**新起 5180 端口 dev server**（run_in_background 直接持有进程，勿用内部 `&` 否则子进程被带走）；已验证 5180 返回 200 且服务端模块含新版代码
- 新地址：**http://localhost:5180/PORTL/**
- 教训：用 Bash `run_in_background` 起长驻服务时，命令本身应是服务进程（不要加 `&` 再 echo，会让子进程随 shell 退出被回收）；用户说「没变化」先查 dev server 是否 HMR 失联/端口堆叠，而非假设代码没改

## 像素级复刻专家模式三态（18:45–19:00）
用户发 3 张设计图要求像素级复刻：
1. **选择模式（设计图1）**：日常办公模式欢迎页 + 模式下拉 + 3 张快捷卡片
2. **选中专家（设计图2）**：蓝色圆角 tag「角色名 ×」在输入框上方、推荐提问 pill 多行标签在输入框下方
3. **hover 专家（设计图3）**：4 列圆形头像卡片网格 + 角色 badge 悬浮头像右下角 + hover 方向性展开 + 底部红字提示

### 改动清单
- **experts.ts** 完全重写 mock 数据：8 位新专家（选品王/飞毛腿/点子王/营收姐/棱高手/老板/文案手/老司机），含 emoji 头像、角色标签色系、推荐提问数组
- **ExpertPicker.vue** 完全重写：圆形 emoji 头像(48px) + 角色标签 badge 浮于头像右下角(rounded-full) + 名字居中加粗 + 描述截断 2 行；hover 展开面板 280px 方向性防溢出（前 2 列右扩/第 3 列居中/第 4 列左扩）；底部 rose-400 红字提示注
- **ChatMessageList.vue** 重构专家模式欢迎区：统一用「🐴 小马在线，有事随时说」标题（不再区分机器人 emoji）；未选专家 → ExpertPicker 渲染在主内容区 max-w-4xl；已选专家 → 仅保留简洁标题（选中 UI 全部移到 ChatInputBox）；日常办公保持 3 张快捷卡片
- **ChatInputBox.vue** 重写选中态 UI：选中专家后输入框**外部上方**显示蓝色 tag（`bg-blue-50 text-blue-700 border-blue-200`）含 × 关闭；输入框**外部下方**展示推荐提问 pill 标签（rounded-full border 多行 flex-wrap）；移除旧的内联横幅和提示文字；placeholder 三模式各自对齐设计图文案
- **useWorkspaceChat.ts** 修 `expert.prompt` → `expert.prompts[0]`（接口变更适配）
- **WorkspaceChatView.vue** 移除 `exp.tags` 渲染（接口字段已删除）
- 验证：typecheck 0 错误 + build 成功（4.07s），dev server 5180 正常

## 浏览器缓存问题终极排查（19:07–19:15）
用户连续3次截图反馈「依旧只有3个旧专家卡片、没变化」，代码已改但浏览器死活不刷新。
- **排查链**：
  1. 磁盘文件 ✅ 含新数据（选品王/飞毛腿/emoji头像）
  2. Vite dev server (5180) curl 验证 ✅ 返回新模块
  3. `--force` 重启 Vite (5199) → 用户仍看旧
  4. **最终方案**：`npm run build` 生产构建 + `npx serve -l 5211` 纯静态服务器
- **根因确认**：Vite HMR WebSocket 断连后，浏览器的 ES module 缓存与 JS 运行时状态完全锁死，即使服务端已返回新模块，浏览器仍执行旧的 module 快照。Cmd+Shift+R、新标签页均无效（同 profile 共享 Service Worker/Cache Storage）
- **解决方案**：生产构建（vite build 打包所有模块为静态 chunk）+ npx serve（零 HMR、无 SW 干扰）+ **无痕窗口打开**
- **教训**：Vite dev server 的 HMR 断连后，唯一可靠的重置方式是「build + 静态托管 + 无痕窗口」；以后改完核心组件直接走这条路径给用户预览
- 当前可用地址：`http://localhost:5211/`（npx serve 托管 dist/，含8位新专家+emoji头像+角色badge）

## PRD 补充：后台基础能力设计（19:34）
用户指出 PRD 缺 MCP/Skill/Function call/模型 四类基础能力（不暴露前端、后台可配），要求"设计清楚逻辑"。
- 新增 4 章 + 1 总览：#28 模型配置 / #29 函数调用 / #30 MCP / #31 技能(Skill) / #32 四者关系与后台配置总览；原 Non-goals 顺延为 #33
- 四层依赖：模型(Model) → 函数调用(Function Call) ← MCP(注册工具进函数池) → 技能(Skill=提示词+工具，绑专家/快捷指令) → 前端普通用户不可见
- 每章严格遵循既有格式：仅一级标题、EARS 四型(各+1→共22)、正常操作流+异常回退流+异常处理、五异常覆盖(断网/空态/骨架屏/非法字符/并发)
- 关键逻辑：模型路由(全局默认+专家覆盖+灰度+降级熔断)、函数调用(权限/参数校验+结果回填+审计)、MCP(注册探活+工具清单+健康检查熔断+隔离授权)、Skill(版本管理+依赖校验+绑定专家)
- 验证：1052 行；一级标题 28-33 无断号；多级 0；视觉风格词 0；EARS 四型各 22 平衡；新章三流+五覆盖齐全
- 路径：`deliverables/product-strategy/prd-portal-agent-2026-07-08.md`

## 31张设计图二次核对与UI改写（19:21起）
用户发31张「天马智擎」设计图要求参照改写 frontend-mvp，先说计划再执行。用户驳回首版计划并指出3处真实问题：
1. **专家模式**：设计图9个专家，用户浏览器只见3个（疑为HMR/缓存旧页，代码实际8个）；角色名是占位乱写（"选品选品专家"重复字）与设计图真实角色不符；交互应为「点击卡片跳转到该专家对应的快捷提示词案例列表」（非toggle选中/hover展开）。
2. **搜索/历史对话弹窗**：用户要求侧边栏的搜索、历史对话都改成弹窗（已有SearchDialog弹窗含X关闭按钮+历史分组列表）；已新增侧边栏「历史对话」图标按钮→openSearchDialog('history')直接展示列表+预览，搜索按钮→openSearchDialog('search')；统一弹窗带关闭按钮。
3. **用户块（金币/通知/头像）不动**。

### 本轮已完成
- 任务1 侧边栏搜索/历史对话弹窗化：composable 加 searchDialogMode('search'|'history') + openSearchDialog(mode)；WorkspaceSidebar 加 History 按钮 + 搜索按钮传 'search'；SearchDialog 加 showMore computed（搜索模式空态显示「更多」，历史模式默认选中首对话并显示预览）；WorkspaceChatView 顶栏/移动端搜索按钮改 'search'。
- 任务3 粘贴文本预览弹窗（对应设计图2）：新建 TextPreviewModal.vue（粘贴文本弹窗，显示文件名+大小+内容，可复制全部/粘贴为上下文）；composable 加 textPreview + openTextPreview/closeTextPreview；ChatInputBox 附件条 txt 点击预览；AttachmentUploadModal 本地/知识中心 txt 文件点击预览；WorkspaceChatView 挂载组件。
- 验证：vue-tsc --noEmit 0 错误。

### 本轮（19:47 起）专家模式重做完成
- 用户文字提供 10 个专家（序号1-10，含名称/花名/形象/JD/场景/具体能力）。**设计图说9个，用户实际列10个——按用户数据实现10个，已反馈让用户确认设计图到底要几个**（疑"市场与竞对专家 老纠"为第10个或设计图数错）。
- **experts.ts 完全重写**：Expert 接口重构为 {name(专家名称)/nickname(花名)/gender/scenes[]/capabilities[]/promptCases[](提示词案例，我基于场景+能力生成3条)/...}，删除 role/prompts/fullDesc；10位专家全部按真实数据落地（选品王/带货一姐/飞毛腿/点子王/解忧姐/大表姐/大客户一哥/操盘手/老王/老纠）。
- **ExpertPicker.vue 交互重做**：网格视图→点击卡片 `enterExpert` 进入该专家「提示词案例列表」视图（头部返回按钮+头像+花名+名称+JD+场景/能力 chip+案例列表）；点击案例 `pickCase`→`chat.applyExpertPrompt(expert, prompt)`（带入输入框+选中专家→欢迎区自动切回对话输入）。hover 面板改为展示 JD+场景+能力+「点击查看提示词案例→」。
- **useWorkspaceChat.ts**：`applyExpertPrompt(expert, prompt)` 签名加 prompt 参数，实现设 chatInput=prompt + selectedExperts=[expert]；接口同步；`toggleExpert` 已不再被 ExpertPicker 调用（保留）。
- **ChatInputBox.vue**：selectedExpertName→nickname(花名)、selectedExpertRole→name(专家名称)；recommendedPrompts 数据源 `e.prompts`→`e.promptCases`。
- **WorkspaceChatView.vue**：更多面板 expert 子列表 `exp.role`→`exp.nickname`。
- 排查：HomeView 用独立 `ExpertAgent` 类型（featuredExpertAgents/selectedExpert），不引用 mock/experts 的 Expert，故其 `.role`/`.prompts` 无需改、不受影响。
- 验证：vue-tsc --noEmit 0 错误；`npm run build` 成功（2523 模块）。
- 预览方案修正：cloudstudio 部署（workbuddy_cloudstudio_deploy）返回 verified:false 且 302 重定向导致 SPA 白屏（WebFetch 仅抓到标题「天马AI」），不可靠。改用 `npx vite preview --port 4321 --host 127.0.0.1`（build 产物、无 HMR）后台运行，curl 验证 HTTP 200 + /assets 绝对路径可达；present_files 传 http://127.0.0.1:4321/ 经内置预览面板打开成功。
- 教训：cloudstudio_deploy 对本项目 SPA + 绝对 asset 路径会 302 白屏，不再作为默认预览；改完核心组件走「vite build + vite preview + present_files localhost」链路。
- 专家数量已定：用户拍板保留 **9 个**，删除第10个「市场与竞对专家 老纠」(exp-10)。experts.ts 现 9 位专家。
- 待做：任务4 样式对齐微调（上传弹窗/字数统计渐变/消息气泡圆角）尚未做。


## PRD 追加：关键成功/错误提示文案（19:48）
用户要求 PRD 最下方加"关键成功/错误提示文案"。
- 新增 #34 章节（位于 #33 Non-goals 之后的最末），按场景分组表格：全局通用/左栏会话/对话生成/右栏预览/专家模式/附件上传/思维链记忆/后台配置（管理员），每项含 成功态/错误态/空态/加载态 文案
- 共 44 条文案条目；明确"不涉及具体视觉样式，仅描述文案与触发时机"
- 验证：1140 行；末章 #34；多级标题 0；视觉风格词 0（已把 intro 中"色彩"一词移除以保零残留）
- 路径：`deliverables/product-strategy/prd-portal-agent-2026-07-08.md`


## PRD 全量重构：3.3.1.1~3.3.1.7 七节模板 + 新模式逻辑（19:53）
用户指出格式又不对，要求每个功能需求内含 3.3.1.1~3.3.1.7 七节（功能需求描述/业务主流程图/界面原型/数据说明/业务规则/接口说明/补充说明），并改逻辑：
- 日常办公模式 = web-search + RAG + 钉钉接入（检索路由→生成→钉钉动作落地）
- 专家模式 = plan and execute（规划→逐步执行→结构化产出）
- 专家清单扩为 10 位（用户给表：选品王/带货一姐/飞毛腿/点子王/解忧姐/大表姐/大客户一哥/操盘手/老王/老纠）
- 方案中心智能体另立规格（本PRD仅引用）
- 保留：参考文档/沟通要求留空、不暴露色彩视觉样式、移除定时任务、默认双栏收起仅搜索框、专家Hover/Click区分
全量重写：1版本/2关于本文档/3功能需求(3.1产品概述/3.2全局要求/3.3功能模块3.3.1~3.3.9七节/3.4总表)/4后台基础能力(4.1-4.4七节+4.5)/5CoT/6Memory/7NFR/8迭代/9指标/10埋点/11文案/12Non-goals
- 验证：1169行；13个模块(3.3.1-3.3.9,4.1-4.4)全部7/7节覆盖；web-search+RAG 6处、plan-and-execute 18处、10位专家表10行；色彩词0；定时任务仅版本说明+Non-goals以"移除"措辞
- 路径：`deliverables/product-strategy/prd-portal-agent-2026-07-08.md`

## PRD 第3次全量重构：8节优化框架 + 14章完整版（20:35）
用户给出优化后的8节框架（版本信息/系统架构图/整体流程图/全局通用规则/功能模块清单/记忆机制/界面原型4列表格/业务规则与约束），要求逐项填充。
- 实际扩展为14章：1版本信息/2系统架构图(mermaid+说明表+后台依赖图)/3整体流程图(mermaid端到端+说明表)/4全局通用规则(4.1~4.7)/5功能模块清单(5.1~5.9各含七节模板+EARS四型+4列表格)/6记忆机制(6.1会话内滑动窗口5轮+6.2跨会话搜索词与专家偏好)/7后台基础能力(7.1模型/7.2函数调用/7.3MCP/7.4技能/7.5四者关系总览)/8思维链CoT(折叠/流式/可关闭)/9非功能需求/10迭代计划(3周Phase)/11成功指标/12数据采集事件清单(18事件)/13关键成功错误提示文案(5成功+27错误)/14 Non-goals(11项)
- 界面原型统一4列：操作|交互|异常处理|截图示例（16处）
- EARS四型各15次（15个功能模块全覆盖）
- 异常处理五覆盖在4.7全局定义，各模块引用
- 色彩/视觉词仅Non-goals否定语境出现1处
- 定时任务仅迭代计划"后续迭代"行+Non-goals NG-1出现（均为"不做"语境）
- 修复7.4.1 EARS表格分隔行列数不匹配
- 最终1442行
- 路径：`deliverables/product-strategy/prd-portal-agent-2026-07-08.md`

## PRD V2.0 全量重写：四级标题层级 + 底座前置 + 八功能模块（最终版）

用户第4次打回，指出之前版本格式未对齐知识中心范本（`docs/需求文档/20260707-【PRD】AI工作台-知识中心V1.0-天马智擎项目.md`），要求严格 `#` → `##` → `###` → `####` 四级标题层级。

### 用户确认大纲
- 3.4 基础能力底座前置（模型/函数/MCP/技能四层 + 记忆机制 + CoT），放在 3.5 功能详情之前
- 功能顺序：整体布局 → 历史会话 → 输入框 → 快捷卡片 → 会话详情 → 文件预览 → 日常办公 → 专家模式
- 删除 3.5.9/3.5.10（跨模式通用两节），附件上传逻辑合并进 3.5.3 对话输入区
- 专家清单移附录（不占功能详情节）
- 架构图简化（5 行主链路 mermaid）
- 每个功能含七节 ####：功能需求描述 / 业务主流程图及说明 / 界面原型 / 数据说明 / 业务规则和约束条件 / 接口说明 / 补充说明

### 文件结构（6 大章）
1. 关于本文档（版本信息V2.0 / 术语 / 参考文档 / 沟通要求）
2. 产品概述（产品定位 / 目标 / 用户角色 / 竞品调研）
3. 功能需求（3.1 全局通用规则 6 节 / 3.2 流程图[架构图+业务流程图] / 3.3 功能模块清单总表 / 3.4 基础能力底座[3.4.1~3.4.8 每个含七节] / 3.5 功能详情[3.5.1~3.5.8 每个含七节]）
4. 非功能需求（性能 / 安全 / 兼容性）
5. 迭代计划（MVP 5 阶段 / 后续迭代 6 项）
6. 附录（6.1 专家清单 10 位 / 6.2 成功指标 / 6.3 数据采集事件 17 个 / 6.4 成功·错误提示文案 / 6.5 Non-goals 12 项）

### 合规校验结果
- 四级标题层级完整：# → ## → ### → #### ✅
- 16 个功能模块 × 7 节 = 112 个 #### 标题，各类型均为 16 ✅
- 界面原型 4 列表格（操作 | 交互 | 异常处理 | 截图示例）16 处 ✅
- EARS 四型覆盖：普遍型 17 / 状态驱动型 16 / 事件驱动型 15 / 不良行为型 16 ✅
- 异常五覆盖：网络断连 7 / 空态 13 / 骨架屏 6 / 非法字符 5 / 并发冲突 7 ✅
- 色彩/视觉词零残留 ✅
- 定时任务仅在 Non-goals ✅
- 3.5.9/3.5.10 已删除 ✅
- 架构图已简化 ✅
- 文件总行数 1499 行
- 路径：`deliverables/product-strategy/prd-portal-agent-2026-07-08.md`

## PRD V2.0 增补：竞品调研 + 架构图优化（21:20）

### 竞品调研补充（2.4 节）
- 用户指出竞品调研报告已存在（`competitive-analysis-portal-agent-2026-07-08.md`），要求补充进 PRD
- 从完整报告精简为 5 个子节填入 2.4：竞品全景（5 个主要竞品 + 国际参考）、关键维度对比表、差异化定位、可借鉴亮点、需规避陷阱
- 主要竞品：钉钉 AI 助理 / 飞书智能伙伴 / Coze 2.0 / Kimi / 通义千问（合规警示）；Notion AI / Copilot 作为国际参考

### 技术架构图优化（3.2.1 节）
- 原图问题：线性链 A→B→C→D→E→F，无视觉分组，"基础能力底座"节点文字过长
- 优化后：使用 subgraph 分层（6 个区域），业务编排层展开 5 个服务节点，基础能力底座展开 4 层内部依赖，外部集成与数据存储并排
- 删除原第二个 mermaid（后台基础能力依赖关系图），其内容已融入主图
- 新增图例说明实线/虚线含义
- 同时用 show_widget 渲染了 SVG 版本供用户直观对比

## PRD 修改优化计划落实：20260709 首页智能体 PRD（V2.1）

针对 `docs/需求文档/20260709-【PRD】AI工作台-首页智能体-天马智擎.md` 落实 9 条修改提示（Superpowers Cn 工作流，主理人直接产出，未 spawn 团队）：

### 关键决策（已与用户确认）
- 记忆窗口：默认 5 轮、后台可调（Q1）
- 知识库范围：企业 + 个人知识库，按用户权限过滤可访问文件（Q2）
- CoT 开关：保留用户级开关（Q3）

### 落实的 9 项
1. 单文件大小统一 50MB（3.5.3 + 6.3.2 错误提示，原为 20MB）
2. 3.2.1 架构图删「实线/虚线」图例（已确认无残留）
3. 3.4 基础底座新增「3.4.5 智能体管理（Agent）」七节（模型/提示词/能力配置/三态生命周期/memoryWindow/接口），原 3.4.5~3.4.8 顺延为 3.4.6~3.4.9
4. 3.4.7 跨会话记忆标注「讨论结论」并按 memoryWindow 决策收口
5. 3.4.9 CoT 改为「思考中详细展示、答后自动折叠、保留用户开关」（原默认折叠）
6. 3.5.3 对话输入区支持知识库文件上传（企业+个人，按权限）
7. 第 5 章迭代计划排 10 步开发顺序（底座前置，仅顺序不含周次，2 周）
8. 6.2 成功指标删除，附录顺延 6.1→6.4（数据采集/文案/Non-goals）
9. 1.3 参考文档补 5 份（知识中心PRD + 工具/模型/评估/智能体管理）

### 结构现状
- 3.4.1~3.4.9 连续无断号；3.5.1~3.5.8；5.1；6.1~6.4
- 3.4.6 五者关系总览已升级为五层依赖（含 Agent 层），mermaid 与 REQ 编号已同步
- 合规：无「四层/四者/20MB」残留；3.4.5 与 3.4.7 记忆窗口口径一致
- 版本表已记 V2.1（2026-07-09）
