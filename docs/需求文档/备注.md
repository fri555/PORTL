# 术语
1. 未确认：同名文件如何处理，不允许上传/后缀增加（数字）--不允许上传
2. 未理解：同一文件实体在不同知识库/文件夹下可以有引用记录-知识库
# 产品概述
1. 不一致：上传文件支持PDF / DOC / DOCX / XLS / XLSX / TXT / MD / CSV
# 三栏布局
1. 不一致：预览面板和小智问答上下/左右排列➡️左右
# 搜索规则
1. 不一致：要求默认全局搜索➡️知识库、文件夹、文件均支持，分组展示
# 操作菜单
1. 未理解：知识库下应该还有新建文件夹菜单✅
2. 未理解：知识库列表页空白处的菜单项是右键二级菜单的逻辑吗➡️按钮
# 文件格式支持
1. 不一致：表格中说明DOC支持预览，备注小字说明不支持DOC预览➡️技术确认
# 文件上传约束
1. 不一致：上传文件任务数量10个一批处理，和前端界面用户交互不冲突，是否要限制用户选择上传文件数量➡️10个
上传顺序➡️添加到列表的时间（正序），先添加的永远在第一行
# 新建知识库
1. 不一致：新建知识库是否需要选择所属空间，会影响toast提示逻辑✅
# 新建文件夹
1. 不一致：没有权限管理，所以只有公共空间的管理员/个人空间有权限新建文件夹
# 文件上传
1. 不理解：只有在知识库中可以看到上传文件按钮，不存在toast逻辑✅
2. 不理解：上传文件是否支持拖拽逻辑 MVP不支持 
3. 不一致：是否展示进度条；是否设立任务中心➡️UI确认
4. 不一致：是否需要敏感词检测➡️MVP不需要
5. 不理解：解析失败两次不可恢复，是否支持手动重试➡️MVP不支持
6. 不一致：排队中是否展示给用户➡️展示
7. 不一致：上传文件过程中不支持重新上传➡️MVP不支持取消
上传任务中心


# 文件搜索
1. 不理解：是否支持搜索知识库
2. 不理解：打开预览面板高亮文件行➡️MVP不需要
# 文件预览
1. 不一致：预览面板不与小智问答强制联动✅
2. 不一致：预览支持标签页逻辑
3. 不理解：高亮3S后清除；大纲/文件内容➡️点击后清除/和大纲和文件内容无关
4. 不一致：目前仅支持单文件问答，是否要支持多文件问答➡️MVP不支持
# 文件删除
1. 不理解：如果文件删除后上传同样文件内容的文件该如何处理➡️不冲突，取消标签的逻辑
# 知识库问答
1. 不理解：是否支持多文件问答
2. 不理解：知识库问答时布局为左右/上下布局

# 其他
1. 文件夹/文件支持移动吗➡️MVP支持，注意考虑文件名重名逻辑