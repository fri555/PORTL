# 用量计费树形下钻与聊天入口优化实施计划

> 按已批准设计，在现有 `frontend-mvp` 中完成管理员/用户差异化用量界面，并修正真实聊天入口。

## 1. 用量计费测试基线

- 扩充 `SettingsInsightsView.spec.ts`，覆盖即时筛选、无结算状态、树节点展开、部门/人员搜索、额度预警、导出和会话抽屉。
- 先运行该测试并确认新增断言失败。

## 2. 管理员树形用量计费

- 重构 `SettingsInsightsView.vue` 的用量数据为部门、人员、模块、子功能、会话层级。
- 实现展开状态、全部展开/收起、搜索自动展开、即时筛选、额度状态和导出。
- 删除结算状态及应用筛选交互。

## 3. 普通用户入口与页面

- 在 `AppHeader.vue` 增加“我的用量”菜单项。
- 通过路由查询参数区分个人视图，确保管理员默认进入组织视图。
- 实现个人指标和模块 → 子功能 → 会话树形明细。

## 4. 真实聊天入口

- 更新 `SourceChatFlows.spec.ts`，覆盖专家模式可选、快捷提示语位于输入框和卡片之间。
- 调整 `SourceChatHome.vue` 和 `QuickPromptPanel.vue` 的结构与模式隔离数据。

## 5. 验证

- 运行相关组件测试，再运行完整测试。
- 运行生产构建。
- 在 5178 页面验证管理员下钻、个人入口、专家模式、快捷提示语与导出反馈。
