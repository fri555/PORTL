from pathlib import Path
import re

source = Path("training_materials_v2/天马智擎平台培训讲稿_四章结构版.md")
target = Path("training_materials_v3/天马智擎平台培训讲稿_正式版.md")
text = source.read_text(encoding="utf-8")

text = text.replace("文档状态：内容审阅版", "文档状态：正式培训版（依据 2026-08-08 正式环境实测）")
text = text.replace("讲解重点：首页智能体、知识中心、仪表盘、工作台", "讲解重点：首页智能体、知识中心、工作台；第三章重点演示数据分析闭环")
text = text.replace("首页、知识中心、仪表盘、工作台四个导航入口的组合截图。", "首页、知识中心、工作台三个培训模块入口的组合截图。")
text = text.replace("第一阶段我们重点交付四组能力：统一对话入口、企业知识使用、专业智能体、数据和系统入口。", "第一阶段本次培训聚焦三组能力：统一对话与专业智能体、企业知识使用、内部系统入口。")

text = re.sub(
    r"### 1\.2\.6 仪表盘能力\n.*?(?=### 1\.2\.7 工作台能力)",
    "",
    text,
    flags=re.S,
)
text = text.replace("### 1.2.7 工作台能力", "### 1.2.6 工作台能力")
text = text.replace("### 1.2.8 模块协同能力", "### 1.2.7 模块协同能力")
text = text.replace("### 1.2.9 截图建议", "### 1.2.8 截图建议")
text = text.replace("制作一张“第一期能力地图”，使用四个界面截图：", "制作一张“第一期能力地图”，使用三个界面截图：")
text = text.replace("3. 仪表盘。\n4. 工作台。", "3. 工作台。")
text = text.replace("首页、知识中心、仪表盘、工作台全流程讲解", "首页、知识中心、工作台全流程讲解")

text = re.sub(
    r"## 2\.3 仪表盘：执行数据与统计信息查看\n.*?(?=## 2\.4 工作台：Agent创建、派发与跟踪)",
    "",
    text,
    flags=re.S,
)
text = text.replace("## 2.4 工作台：Agent创建、派发与跟踪", "## 2.3 工作台：内部系统统一入口")
text = text.replace("### 2.4.", "### 2.3.")
text = text.replace("本次测试账号可见 10 个系统入口。", "本次正式环境实测账号可见 11 个系统入口。")

data_section = r'''## 3.3 数据分析：从问题定义到结果沉淀

### 3.3.1 场景定位

**【讲师说】**

数据分析不是把一个表扔给 AI，再问一句“帮我看看”。真正可复用的流程是：先定义业务问题，再检查数据和口径，选择合适的上传路径，选择数据分析专家，写清分析任务，观察执行过程，核验结果，最后把结论沉淀为报告、待办或知识。

当前正式环境的专家模式中已提供“数据分析”智能体，页面说明为“输出经营报表、诊断异常指标、提供数据洞察”。它适合处理有明确数据文件、分析目标和核验方法的任务。

### 3.3.2 第一步：先定义业务问题

先回答四个问题：

1. 要支持什么业务决策，例如月度复盘、商品诊断、渠道对比或异常排查。
2. 分析范围是什么，例如 7 月、华东区、B2C 渠道和指定品类。
3. 需要哪些指标与维度，例如销量、销售额、评分，按周、区域、渠道和 SKU 拆分。
4. 最终要什么产物，例如管理摘要、明细表、风险清单或汇报提纲。

### 3.3.3 第二步：检查数据是否可分析

**【现场操作】**

1. 打开原始表，核对表头、单位、时间格式和主键。
2. 说明空值、重复值、合并单元格、公式和异常编码。
3. 确认字段含义，例如“销售额”是否含税、退货是否已扣除。
4. 删除与任务无关且不应上传的敏感字段。
5. 保留一个可回查的原始版本，不要只保留清洗后的文件。

**【边界提醒】**

AI 能够帮助发现问题，但不能替代数据治理。字段缺失、口径不一致或表格结构复杂时，回答可能完整但结论仍然错误。

### 3.3.4 第三步：选择文件路径

- 临时、少量、一次性使用的 PDF、Word、Excel 或文本：从首页直接上传，单文件不超过 10MB，最多 3 个附件。
- 较大、需要多人复用或后续持续提问的材料：先上传知识中心，等待处理成功，再从首页引用。
- CSV 不能从首页直接上传；当前页面提示知识中心支持 CSV。需要分析 CSV 时，优先走知识中心，或在确认格式无误后转换为 XLSX。

### 3.3.5 第四步：选择数据分析专家并挂载文件

**【现场操作】**

1. 首页选择“专家模式”。
2. 在“数据与洞察”中选择“数据分析”。
3. 点击“添加附件”。
4. 上传 XLS/XLSX，或从知识中心选择已经处理成功的文件。
5. 发送前再次核对文件名、大小和版本，错误附件立即移除。

### 3.3.6 第五步：用六要素写分析任务

推荐提示词包含六项：业务目标、数据范围、统计口径、分析维度、输出结构、核验要求。

**【演示提示词】**

> 请分析附件中的模拟经营数据。业务目标是识别销量与评分的异常变化；数据范围为附件全部记录；按周、区域、渠道和品类汇总销量与销售额；列出异常点及对应的数据证据；说明需要人工核验的口径和缺失字段；最后给出不超过 5 条可执行建议。所有结论必须引用附件中的具体数据，不要补造数字。

### 3.3.7 第六步：查看执行过程和异常状态

发送后先确认三件事：文件是否成功挂载、智能体是否正确、任务是否进入执行。复杂任务可以展开思考或处理过程，了解系统是否读取了文件、调用了哪些能力。

本次正式环境实测中，模拟 XLSX 文件能够成功上传和发送，但数据分析智能体返回“智能体执行异常，请稍后重试”。这说明第一版仍可能出现执行失败。遇到此类情况，应先保留会话和截图，核对文件格式、网络状态和任务描述，必要时重新编辑或重试；如果仍失败，通过“我要吐槽”跳转钉钉反馈，不要把执行失败误当作“数据没有问题”。

### 3.3.8 第七步：核验结果，而不是只看结论

结果返回后至少核验：

1. 汇总行数和总量是否能在原表复算。
2. 环比、占比和平均值的分母是否正确，首期没有参照值时是否仍错误计算环比。
3. 异常点是否有具体日期、维度和数值证据。
4. 是否把相关性写成因果关系。
5. 数据不足时是否明确说明，而不是补造数字。
6. 建议动作是否超出当前数据能够支持的范围。

### 3.3.9 第八步：重新编辑、重试和沉淀输出

- 任务范围或口径写错：使用重新编辑，修正原问题后再次执行。
- 问题不变、执行偶发失败：使用重试；重试前先检查文件状态和网络。
- 需要保留旧结果：先复制或下载，再重新编辑，因为后续回答可能被替换。
- 结果核验通过：复制为汇报提纲，下载生成文件，或在日常办公中继续整理消息、待办和会议材料。
- 可复用的数据说明、口径和优秀提示词：脱敏后沉淀到知识中心。

### 3.3.10 截图建议

- 数据分析专家卡片和能力说明。
- 模拟 XLSX 上传成功状态。
- 六要素提示词。
- 发送后的处理过程或异常提示。
- 重新编辑、重试、复制和反馈入口。
'''
text = re.sub(
    r"## 3\.3 数据查询：基础数据分析与报表生成\n.*?(?=## 3\.4 日常办公)",
    data_section + "\n",
    text,
    flags=re.S,
)

text = text.replace("首页、知识中心、仪表盘或工作台。", "首页、知识中心或工作台。")
text = text.replace("组货、评价分析、知识查询、建日程或查指标", "组货、评价分析、数据分析、知识查询或建日程")
text = text.replace("平台中的“问题反馈”入口", "平台右上角的“我要吐槽”入口")
text = text.replace("平台“问题反馈”入口", "平台“我要吐槽”入口")
text = text.replace("点击平台“问题反馈”入口", "点击平台“我要吐槽”入口")

# Remove dashboard-specific appendix lines and QA remnants.
text = re.sub(r"## A\.4 仪表盘和工作台\n.*?(?=## A\.5)", "## A.4 工作台\n\n1. 工作台全景。\n2. 系统搜索与卡片。\n3. ‘我要吐槽’入口。\n\n", text, flags=re.S)
text = text.replace("四大模块导航", "三大培训模块导航")
text = "\n".join(line for line in text.splitlines() if "仪表盘" not in line) + "\n"

target.write_text(text, encoding="utf-8")
