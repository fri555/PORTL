# 天马智擎 HTML 培训课件

双击或用浏览器打开 `index.html` 即可播放。

- `←` / `→`：上一页 / 下一页
- `S`：演讲者视图（显示当前页、下一页、讲稿和计时）
- `F`：全屏
- `O`：总览

课件按 1920×1080（16:9）设计，截图来自正式环境实测并已用红框和箭头标注。`screenshots` 与 `assets` 文件夹需和 `index.html` 保持在同一目录层级。
